import { formatLastminutes } from '../../../helpers/format-lastminutes';
import { module, test } from 'qunit';

module('Unit | Helper | format lastminutes');

// Replace this with your real tests.
test('it works', function(assert) {
  var result = formatLastminutes(42);
  assert.ok(result);
});
